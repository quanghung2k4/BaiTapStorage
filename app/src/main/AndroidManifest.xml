<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BaiTapStorage">
        <activity
            android:name=".activities.LoginActivity"
            android:exported="false" />
        <activity
            android:name=".activities.MoviesActivity"
            android:exported="false"
            android:label="Movies" />
        <activity
            android:name=".activities.TheatersActivity"
            android:exported="false"
            android:label="Theaters" />
        <activity
            android:name=".activities.ShowtimesActivity"
            android:exported="false"
            android:label="Showtimes" />
        <activity
            android:name=".activities.SeatSelectActivity"
            android:exported="false"
            android:label="Select Seat" />
        <activity
            android:name=".activities.TicketResultActivity"
            android:exported="false"
            android:label="Booking Confirmed" />
        <activity
            android:name=".activities.MyTicketsActivity"
            android:exported="false"
            android:label="My Tickets" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>